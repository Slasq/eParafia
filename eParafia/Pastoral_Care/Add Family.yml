info:
  name: Add Family
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/families
  body:
    type: json
    data: |-
      {
        "familyName": "Kowalscy",
        "memberCount": 4
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 201", function() {
            expect(res.getStatus()).to.equal(201);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
