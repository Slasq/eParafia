info:
  name: Edycja ogłoszenia
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/announcements/2"
  body:
    type: json
    data: |-
      {
        "content": "Zmieniona treść ogłoszenia parafialnego"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
