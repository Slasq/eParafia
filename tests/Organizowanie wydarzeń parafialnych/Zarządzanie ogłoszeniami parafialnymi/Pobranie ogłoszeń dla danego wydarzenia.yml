info:
  name: Pobranie ogłoszeń dla danego wydarzenia
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/announcements?eventId=1"
  params:
    - name: eventId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
