info:
  name: Pobranie  konkretnego ogłoszenia
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/announcements/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
