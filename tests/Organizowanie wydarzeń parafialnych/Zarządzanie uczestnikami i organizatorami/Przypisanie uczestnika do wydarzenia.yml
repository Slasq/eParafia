info:
  name: Przypisanie uczestnika do wydarzenia
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/participants"
  body:
    type: json
    data: |-
      {
        "firstName": "Karolina",
        "lastName": "Majarz",
        "role": "uczestnik wydarzenia",
        "eventId": 7
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
