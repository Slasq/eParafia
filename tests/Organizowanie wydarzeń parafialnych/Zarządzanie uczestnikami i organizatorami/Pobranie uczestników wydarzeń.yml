info:
  name: Pobranie uczestników wydarzeń
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/participants"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
