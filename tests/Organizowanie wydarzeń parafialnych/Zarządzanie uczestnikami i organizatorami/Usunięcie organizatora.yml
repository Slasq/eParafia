info:
  name: Usunięcie organizatora
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{baseUrl}}/api/organizers/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
