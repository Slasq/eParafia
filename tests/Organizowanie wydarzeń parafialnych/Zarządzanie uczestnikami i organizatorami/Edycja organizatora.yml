info:
  name: Edycja organizatora
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{baseUrl}}/api/organizers/1"
  body:
    type: json
    data: |-
      {
        "firstName": "Robert",
        "lastName": "Maj",
        "role": "główny koordynator wydarzenia",
        "eventId": 6
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
