info:
  name: Pobranie listy intencji
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/intentions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
