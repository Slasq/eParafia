info:
  name: Pobranie konkretnej intencji
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/intentions/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
