info:
  name: List Parishes
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/parishes
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 200", function() {
            expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
