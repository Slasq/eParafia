info:
  name: Dodanie pracownika do wybranej parafii
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/employees"
  body:
    type: json
    data: |-
      {
        "firstName": "Kamil",
        "lastName": "Gorski",
        "positionId": 5,
        "parishId": 8
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
