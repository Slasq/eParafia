info:
  name: Pobranie pracowników z danej parafii
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/employees?parishId=8"
  params:
    - name: parishId
      value: "8"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
