info:
  name: Pobranie wszystkich parafii
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/parishes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
