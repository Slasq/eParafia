info:
  name: Pobranie wszystkich stanowisk
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/positions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
