info:
  name: Pobranie obowiązków dla danego stanowiska
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/duties?positionId=2"
  params:
    - name: positionId
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
