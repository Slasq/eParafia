info:
  name: Pobranie wszystkich obowiązków
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/duties"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
