info:
  name: Usunięcie rodziny
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/families/12"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
