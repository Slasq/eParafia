info:
  name: Pobranie wszystkich rodzin
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/families"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
