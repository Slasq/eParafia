info:
  name: Dodanie nowej rodziny
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/families"
  body:
    type: json
    data: |-
      {
        "familyName": "Majewscy",
        "memberCount": 4
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
