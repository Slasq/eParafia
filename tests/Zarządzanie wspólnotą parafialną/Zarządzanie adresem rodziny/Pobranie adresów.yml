info:
  name: Pobranie adresów
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/family-addresses"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
