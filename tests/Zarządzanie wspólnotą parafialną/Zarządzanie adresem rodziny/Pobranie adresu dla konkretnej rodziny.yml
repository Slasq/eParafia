info:
  name: Pobranie adresu dla konkretnej rodziny
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/family-addresses?familyId=8"
  params:
    - name: familyId
      value: "8"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
