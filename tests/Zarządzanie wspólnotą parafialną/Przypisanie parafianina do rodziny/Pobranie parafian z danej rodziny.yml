info:
  name: Pobranie parafian z danej rodziny
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/parishioners?familyId=2"
  params:
    - name: familyId
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
