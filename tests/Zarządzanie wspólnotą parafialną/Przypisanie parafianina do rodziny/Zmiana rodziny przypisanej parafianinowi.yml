info:
  name: Zmiana rodziny przypisanej parafianinowi
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/parishioners/14"
  body:
    type: json
    data: |-
      {
        "familyId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
