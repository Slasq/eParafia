info:
  name: Usunięcie miejscowości
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/localities/11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
