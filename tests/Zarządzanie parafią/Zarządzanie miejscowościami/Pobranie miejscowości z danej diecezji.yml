info:
  name: Pobranie miejscowości z danej diecezji
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/localities?dioceseId=1"
  params:
    - name: dioceseId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
