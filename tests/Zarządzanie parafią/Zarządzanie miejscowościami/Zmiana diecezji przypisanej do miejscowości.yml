info:
  name: Zmiana diecezji przypisanej do miejscowości
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/localities/10"
  body:
    type: json
    data: |-
      {
        "dioceseId": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
