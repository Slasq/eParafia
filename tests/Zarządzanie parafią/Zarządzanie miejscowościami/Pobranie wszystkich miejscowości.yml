info:
  name: Pobranie wszystkich miejscowości
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/localities"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
