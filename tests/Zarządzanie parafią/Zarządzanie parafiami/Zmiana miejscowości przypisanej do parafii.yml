info:
  name: Zmiana miejscowości przypisanej do parafii
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/parishes/11"
  body:
    type: json
    data: |-
      {
        "localityId": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
