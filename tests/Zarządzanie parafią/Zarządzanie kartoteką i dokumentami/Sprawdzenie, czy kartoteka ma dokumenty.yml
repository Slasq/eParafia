info:
  name: Sprawdzenie, czy kartoteka ma dokumenty
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/documents?recordId=11"
  params:
    - name: recordId
      value: "11"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
