info:
  name: Pobranie wszystkich kartotek
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/records"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
