info:
  name: Pobranie dokumentów dla konkretnej kartoteki
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/documents?recordId=1"
  params:
    - name: recordId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
