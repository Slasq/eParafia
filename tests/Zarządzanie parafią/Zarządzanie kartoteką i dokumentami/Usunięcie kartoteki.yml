info:
  name: Usunięcie kartoteki
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{baseUrl}}/api/records/11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
