info:
  name: Usunięcie dokumentu
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/documents/11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
