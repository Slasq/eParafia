info:
  name: Pobranie wybranej diecezji
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/dioceses/6"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
