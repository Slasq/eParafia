info:
  name: Usunięcie diecezji
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/dioceses/11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
