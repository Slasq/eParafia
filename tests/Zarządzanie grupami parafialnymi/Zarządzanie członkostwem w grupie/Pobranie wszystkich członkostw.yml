info:
  name: Pobranie wszystkich członkostw
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/memberships"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
