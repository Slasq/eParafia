info:
  name: Usunięcie grupy parafialnej
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/groups/11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
