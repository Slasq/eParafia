info:
  name: Pobranie wszystkich grup parafialnych
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/groups"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
