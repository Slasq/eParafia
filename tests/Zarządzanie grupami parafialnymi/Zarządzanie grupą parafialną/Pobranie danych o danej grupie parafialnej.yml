info:
  name: Pobranie danych o danej grupie parafialnej
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/groups/10"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
