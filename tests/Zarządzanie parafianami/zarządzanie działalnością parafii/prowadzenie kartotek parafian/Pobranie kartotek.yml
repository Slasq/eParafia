info:
  name: Pobranie kartotek
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/records"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
