info:
  name: Edycja opisu kartoteki
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/api/records/10"
  body:
    type: json
    data: |-
      {
        "description": "Zaktualizowany opis kartoteki parafianina po edycji danych w teście Bruno"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
