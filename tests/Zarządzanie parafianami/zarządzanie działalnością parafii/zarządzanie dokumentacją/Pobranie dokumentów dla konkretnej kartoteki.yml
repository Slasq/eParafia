info:
  name: Pobranie dokumentów dla konkretnej kartoteki
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/documents?recordId=4"
  params:
    - name: recordId
      value: "4"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
